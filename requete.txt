Create Table Support (
    nom_support VARCHAR(30),
    PRIMARY KEY (nom_support)
);

Create Table Categorie (
    nom_categorie VARCHAR(30),
    PRIMARY KEY (nom_categorie)
);

Create Table Jeu (
    nom VARCHAR(30),
    prix DECIMAL(5,2),
    synopsis VARCHAR(40),
    id_jeu BIGINT AUTO_INCREMENT,
    PRIMARY KEY (id_jeu)
);

Create Table Utilisateur (
    Role VARCHAR(10),
    login VARCHAR(30),
    mdp VARCHAR(30),
    date_inscription DATE,
    date_derniere_connexion DATETIME,
    nom VARCHAR(30),
    prenom VARCHAR(20),
    date_naissance DATE,
    adresse_email VARCHAR(320),
    chemin_pdp VARCHAR(50),
    PRIMARY KEY (login)
);

Create Table Article (
    titre VARCHAR(30),
    contenu VARCHAR(500),
    note DECIMAL(3,2),
    caracteristiques VARCHAR(100),
    date_creation DATETIME,
    date_modification DATETIME,
    id_article BIGINT AUTO_INCREMENT,
    id_jeu BIGINT AUTO_INCREMENT,
    PRIMARY KEY (id_article, id_jeu),
    FOREIGN KEY(id_jeu) References Jeu(id_jeu)
);

Create Table Avis (
    titre VARCHAR(30),
    texte VARCHAR(500),
    note DECIMAL(3,2),
    date_creation DATETIME,
    id_avis BIGINT AUTO_INCREMENT,
    id_article BIGINT,
    login VARCHAR(30),
    PRIMARY KEY (id_article, login),
    FOREIGN KEY(id_article) References Article(id_article),
    FOREIGN KEY(login) References Utilisateur(login)
);

Create Table Image (
    chemin_image VARCHAR(50),
    id_article BIGINT AUTO_INCREMENT,
    PRIMARY KEY (chemin_image, id_article),
    FOREIGN KEY(id_article) References Article(id_article)
);

Create Table Est_jouable_sur (
    id_jeu BIGINT AUTO_INCREMENT,
    nom_support VARCHAR(50),
    PRIMARY KEY (id_jeu, nom_support),
    FOREIGN KEY(id_jeu) References Jeu(id_jeu),
    FOREIGN KEY(nom_support) References Support(nom_support)
);

Create Table Est_categorise_par (
    id_jeu BIGINT AUTO_INCREMENT,
    nom_categorie VARCHAR(50),
    PRIMARY KEY (id_jeu, nom_categorie),
    FOREIGN KEY(id_jeu) References Jeu(id_jeu),
    FOREIGN KEY(nom_categorie) References Categorie(nom_categorie)
);

Create Table Administre (
    login VARCHAR(30),
    id_article BIGINT AUTO_INCREMENT,
    PRIMARY KEY (login, id_article),
    FOREIGN KEY(login) References Utilisateur(login),
    FOREIGN KEY(id_article) References Article(id_article)
);